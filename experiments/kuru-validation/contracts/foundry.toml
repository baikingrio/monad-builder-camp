[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["../../monad-lucky-draw/contracts/lib"]
solc_version = "0.8.28"

# Local-only validation spike. No RPC or broadcast configuration is included.
