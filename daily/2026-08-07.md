# 2026-08-07｜Week 5：OddLane 公开 Demo 上线并提交 Monad Hackathon Final Submission

## 今日目标

今天将 OddLane 从“本地验证与私有开发进度”推进到可访问的公开 Demo，并完成 Monad Builder Camp 的 Week 5 最终项目提交。

OddLane 仍是个人独立开发的 Monad Testnet 项目，主线保持不变：

```text
Prediction-native Fully Onchain CLOB
+ Complete Set
+ Pyth BTC/USD Anchor / Settlement
+ Single-Lane Rollover
+ ZeroDev Kernel Trading Account
```

## 公开 Demo 已可访问

OddLane 已部署到 Vercel：

- 首页：https://oddlane.vercel.app/
- 交易页：https://oddlane.vercel.app/markets

今天实际打开并检查了公开交易页。页面可以读取当前市场状态、目标价、BTC 参考价、倒计时、Above / Below 订单簿、下单界面和交易账户入口。

本次检查时，市场已完成锚定并进入交易窗口。页面明确标示“实时参考价仅用于展示；结算以链上验证的价格为准”，没有将 Pyth Reference Price 伪装成真实成交价或订单簿深度。

## 公开 Demo 与真实交易边界

公开页面可用于理解 OddLane 的核心交互，但应如实区分验证层级：

```text
已公开访问：市场状态、目标价、行情展示、订单簿、下单和账户 UI
已在 Testnet 验证：Kernel Owner UserOperation 与 P0/P1 资金闭环
本地验证：合约与前端测试、production build
本次浏览器检查未执行：连接钱包后的真实下单
```

本次检查时订单簿没有可见买卖深度或成交。因此不能将页面当前状态描述成已有自然市场流动性。Demo Bot、Keeper 与用户交易账户保持独立，Demo 流动性也不能代替协议或用户资产。

## Final Submission 已提交

今天已向 WCB 提交：

```text
Week 5｜Hackathon Final Submission｜提交你的 Monad Demo
状态：SUBMITTED，等待审核
```

提交材料说明了：

- OddLane 面向固定 15 分钟 BTC/USD 目标价预测市场的用户问题；
- Complete Set、Prediction-native CLOB、Pyth 结算和 Single-Lane Rollover 的 Monad 使用方式；
- 我作为独立开发者负责产品、合约、测试网验证、前端、ZeroDev Kernel 和部署；
- 已真实实现的部分，以及 Demo / Session / 流动性相关的已知限制。

## 当前质量状态

当前版本已经在本地重新验证：

```text
合约：forge test -vvv
79 passed, 0 failed

前端：
pnpm test
196 passed, 0 failed

pnpm run build
passed
```

production build 仍有待处理告警：

```text
- Bot / Taker 与 rpc-resilience util 的重复自动导入
- es2019 target 下 BigInt literal 的运行时兼容性风险
```

## 已知限制

```text
- 当前为 Monad Testnet Demo，不使用主网或真实资金；
- 订单簿公开实例当前没有可见自然深度和成交；
- Session Gas Sponsorship 尚未通过连续成功 Gate，不能宣传稳定免 Gas；
- Session Permission 的真实 place / cancel / redeem、越权拒绝、过期与 Owner 撤销 E2E 仍待补齐；
- 已完成 Final Submission，但当前仅为 SUBMITTED，不等于审核通过。
```

## 下一步

```text
1. 修复 duplicate-import 与 BigInt target 告警；
2. 用独立演示钱包录制领水 → 入金 → 下单 / 撤单 → 持仓的完整流程；
3. 补 Session Permission 的真实 Testnet E2E 和撤销证据；
4. 让 Demo Bot、Keeper、用户钱包以独立角色完成可复现彩排；
5. 跟踪 WCB 最终提交的审核状态，并准备 Demo Day 展示材料。
```

## 公开学习记录边界

OddLane 合约与前端为私有开发仓库。本记录仅公开可访问 Demo、验证结论、测试汇总、Known Issues 和后续计划；不公开私有源码、部署配置、环境变量、Ops Token、Session Key、交易哈希或任何私钥。
